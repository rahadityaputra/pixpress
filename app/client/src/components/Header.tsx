import { Heading } from "@radix-ui/themes";

const Header = () => {
  return (
    <header className="w-full p-3 z-10 font-bebas-neue text-3xl font-semibold">
      <Heading as="h1">Pixpress</Heading>
    </header>
  );
};

export default Header;
